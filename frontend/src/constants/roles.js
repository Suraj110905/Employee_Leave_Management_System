/**
 * Centralized User Roles.
 * Standard role strings used for permission checks and layout mappings.
 */
export const ROLES = {
  EMPLOYEE: "employee",
  MANAGER: "manager",
  HR_ADMIN: "hr_admin",
};

export default ROLES;
